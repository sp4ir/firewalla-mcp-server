# Test environment variables for Jest test suite
# SAFE TO COMMIT: These are mock credentials for testing only - NOT production values
# These variables are used only during automated testing and have no access to real systems

FIREWALLA_MSP_TOKEN=mock-test-token-for-jest-suite
FIREWALLA_MSP_ID=mock-test-msp-domain
FIREWALLA_BOX_ID=mock-test-box-id-12345

# Optional test configuration
API_TIMEOUT=5000
API_RATE_LIMIT=1000
CACHE_TTL=60
LOG_LEVEL=error